@model RIAT.DAL.Entity.Models.Doenca
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer;
@{
    ViewData["Title"] = @Localizer["Title"];
}

<h1>@Localizer["PageType"]</h1>

<div>
    <h4>@ViewData["Title"]</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DoenCodDoenca)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DoenCodDoenca)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DoenNmHipotese)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DoenNmHipotese)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Administradores"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id">@Localizer["btnEdit"]</a>
    }
    | <a asp-action="Index">@Localizer["btnBackList"]</a>
</div>

@model RIAT.DAL.Entity.Models.AlternativaAtendimento
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer;
@{
    ViewData["Title"] = @Localizer["Title"];
}

<h1>@Localizer["PageType"]</h1>

<div>
    <h4>@ViewData["Title"]</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Estado)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Estado)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IdAlternativaAdlNavigation)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IdAlternativaAdlNavigation.Codigo)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IdAtendimentoNavigation)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IdAtendimentoNavigation.AtenStAgendamento)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Profissionais") || User.IsInRole("Administradores"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id">@Localizer["btnEdit"]</a>
    }
    | <a asp-action="Index">@Localizer["btnBackList"]</a>
</div>

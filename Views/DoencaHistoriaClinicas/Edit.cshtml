@model RIAT.DAL.Entity.Models.DoencaHistoriaClinica

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DoencaHistoriaClinica</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="HiclIdHistoriaClinica" class="control-label"></label>
                <select asp-for="HiclIdHistoriaClinica" class="form-control" asp-items="ViewBag.HiclIdHistoriaClinica"></select>
                <span asp-validation-for="HiclIdHistoriaClinica" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DoenIdDoenca" class="control-label"></label>
                <select asp-for="DoenIdDoenca" class="form-control" asp-items="ViewBag.DoenIdDoenca"></select>
                <span asp-validation-for="DoenIdDoenca" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

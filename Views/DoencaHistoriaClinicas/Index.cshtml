@model IEnumerable<RIAT.DAL.Entity.Models.DoencaHistoriaClinica>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HiclIdHistoriaClinicaNavigation.PaciIdPacienteNavigation.NomePaciente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HiclIdHistoriaClinicaNavigation.HiclNmPlanoSaude)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DoenIdDoencaNavigation.DoenCodDoenca)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HiclIdHistoriaClinicaNavigation.PaciIdPacienteNavigation.NomePaciente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HiclIdHistoriaClinicaNavigation.HiclNmPlanoSaude)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DoenIdDoencaNavigation.DoenCodDoenca)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
